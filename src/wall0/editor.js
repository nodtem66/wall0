define(['wall0/lib'],function(lib){	//Private	var doc = this.document	;		//Public	return function(id){		var text_editor_elem;		if(typeof id == 'string')		{			if(id == '')				return;			else if(id.slice(0,1) == '#')				id = id.slice(1);			text_editor_elem = doc.getElementById(id);			if(text_editor_elem == undefined)			{				throw('wall0 > NOT FOUND id: '+id);				return;			}		} else {			text_editor_elem = id;		}		var df = doc.createDocumentFragment();		console.log('wall0 > init complete with id: '+id);	}});